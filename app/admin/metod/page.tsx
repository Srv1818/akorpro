import { AdminMetodClient } from "@/components/admin/admin-metod-client";

export const metadata = { title: "Admin — Metodlar" };

export default function AdminMetodPage() {
  return <AdminMetodClient />;
}

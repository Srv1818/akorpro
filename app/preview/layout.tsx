/** Önizleme rotası artık kök layout ile aynı neu temayı kullanır; ek sarmalayıcı yok. */
export default function PreviewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
